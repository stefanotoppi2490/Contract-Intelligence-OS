export default function LedgerPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Ledger</h1>
      <p className="text-muted-foreground">Risk ledger feed (coming in a later step).</p>
    </div>
  );
}
